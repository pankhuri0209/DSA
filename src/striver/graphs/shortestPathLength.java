package striver.graphs;

import java.util.HashSet;
import java.util.Queue;
import java.util.Set;

public class shortestPathLength {

    public void shortestPathLength(int[][] graph)
    {
        if (graph.length ==1)
        {
           // return 0;

        }
        int n= graph.length;
        Set<Integer> [] visited = new HashSet[n];
        System.out.println();
       // Queue<Node>
    }
}
